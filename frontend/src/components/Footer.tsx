import {
  ButtonContainer,
  FooterContainer,
  KeyPhrase,
  MainText,
} from '../styles/Footer.style';

export const Footer = () => {
  return (
    <FooterContainer>
      <KeyPhrase>Online Core Time</KeyPhrase>
    </FooterContainer>
  );
};
