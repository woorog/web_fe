import { texts } from './SolveList';

export const initialCurrentIndex = Math.floor(Math.random() * texts.length);
export const initialFade = 'opacity-100';
export const initialRecentIndices: number[] = [];